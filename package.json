{
  "name": "nzm-stats-worker",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "start": "wrangler dev --ip 0.0.0.0",
    "deploy": "wrangler deploy",
    "build": "echo 'No build required for Cloudflare Worker'"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
